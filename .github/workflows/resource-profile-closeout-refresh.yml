name: Resource profile closeout refresh

on:
  push:
    branches: [dev]

permissions:
  contents: write

jobs:
  refresh:
    if: github.event.head_commit.message != 'Refresh resource profile closeout catalog'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Refresh and validate
        run: |
          python refs/tools/generate_source_catalog.py
          python refs/tools/generate_okf_indexes.py
          python refs/tools/check_case_collisions.py
          python refs/tools/generate_source_catalog.py --check
          python refs/tools/generate_okf_indexes.py --check
          python refs/tools/validate_refs.py --mode initialized
          python scripts/agent_context.py --focus "backup pre-migration snapshot sqlite WAL module data retention verification restore eligibility path containment" --issue 65
      - name: Commit refresh and remove helper
        run: |
          rm .github/workflows/resource-profile-closeout-refresh.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Refresh resource profile closeout catalog"
          git push origin HEAD:dev
