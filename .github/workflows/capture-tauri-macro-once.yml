name: Capture Tauri Macro Once

on:
  push:
    branches:
      - agent/issue-5-desktop-clean

permissions:
  contents: write

jobs:
  diagnose:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/issue-5-desktop-clean
      - name: Install Tauri system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            build-essential \
            curl \
            wget \
            file \
            libxdo-dev \
            libssl-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev
      - uses: dtolnay/rust-toolchain@stable
      - name: Capture macro error
        run: |
          set +e
          cargo check --manifest-path desktop/src-tauri/Cargo.toml > /tmp/tauri-check.txt 2>&1
          status=$?
          tail -n 80 /tmp/tauri-check.txt > refs/testing/tauri-macro-diagnostic.txt
          echo "exit_status=$status" >> refs/testing/tauri-macro-diagnostic.txt
          rm .github/workflows/capture-tauri-macro-once.yml
      - name: Commit diagnostic
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'test: capture Tauri macro diagnostic'
          git push origin HEAD:agent/issue-5-desktop-clean
