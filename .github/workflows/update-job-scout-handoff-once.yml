name: Update Job Scout handoff once

on:
  push:
    branches: [dev]
    paths:
      - .github/workflows/update-job-scout-handoff-once.yml

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install index dependency
        run: python -m pip install pyyaml
      - name: Update handoffs
        run: python .github/update_job_scout_handoff_once.py
      - name: Remove helper and refresh OKF indexes
        run: |
          git rm .github/update_job_scout_handoff_once.py .github/workflows/update-job-scout-handoff-once.yml
          python refs/tools/generate_okf_indexes.py
      - name: Commit handoff
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Record acquisition persistence and provider fixes"
          git push origin dev
