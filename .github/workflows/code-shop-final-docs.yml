name: Code Shop final docs refresh

on:
  push:
    branches: [dev]

permissions:
  contents: write

jobs:
  refresh:
    if: github.event.head_commit.message != 'Refresh final Code Shop handoff indexes'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install
        run: python -m pip install -e ".[dev]"
      - name: Refresh and validate deterministic docs indexes
        run: |
          python refs/tools/generate_okf_indexes.py
          python refs/tools/check_case_collisions.py
          python refs/tools/generate_source_catalog.py --check
          python refs/tools/generate_okf_indexes.py --check
          python refs/tools/validate_refs.py --mode initialized
          python scripts/agent_context.py --focus "manager attention review queue dependency blocking Code Shop escalation" --issue 61
      - name: Commit refreshed indexes and remove helper
        run: |
          rm .github/workflows/code-shop-final-docs.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "Refresh final Code Shop handoff indexes"
          git push origin HEAD:dev
