{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core desktop permissions for the Nerve Center main window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
